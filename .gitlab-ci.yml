cache:
  paths:
    - ~/.cache/pip
    - ~/.platformio/.cache

stages:
  - build
  - embedded_build
  - deploy

.assume_role: &assume_role
    - >
      STS=($(aws sts assume-role-with-web-identity
      --role-arn ${ROLE_ARN}
      --role-session-name "GitLabRunner-${CI_PROJECT_ID}-${CI_PIPELINE_ID}"
      --web-identity-token $ID_TOKEN
      --duration-seconds 3600
      --query 'Credentials.[AccessKeyId,SecretAccessKey,SessionToken]'
      --output text))
    - export AWS_ACCESS_KEY_ID="${STS[0]}"
    - export AWS_SECRET_ACCESS_KEY="${STS[1]}"
    - export AWS_SESSION_TOKEN="${STS[2]}"


build artifact:
  stage: build
  image: node:latest
  before_script:
    - cd GardenGlowDesignFrontend
    - mkdir -p ./build
    - npm install
    # Allow warnings to pass a merge request pipeline build.
    - if [ "$CI_PIPELINE_SOURCE" == "merge_request_event" ]; then export CI=false; fi
  script:
    - npm run build > build_output.log 2>&1
    - |
      if grep -i "warning" build_output.log; then
        echo "Warnings found during build";
        curl --header "PRIVATE-TOKEN: $CI_JOB_TOKEN" --data "body=Build warnings found: $(cat build_output.log)" "$CI_API_V4_URL/projects/$CI_PROJECT_ID/merge_requests/$CI_MERGE_REQUEST_IID/notes";
        exit 1;
      fi
    # TODO: Add linting and testing
    # - if [ "$CI_COMMIT_BRANCH" == "main" ]; then npm run lint && npm run test; fi
  artifacts:
    paths:
      - ./GardenGlowDesignFrontend/build/
    when: always
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - GardenGlowDesignFrontend/**/*
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes:
        - GardenGlowDesignFrontend/**/*

deploy s3:
  stage: deploy
  image:
    name: amazon/aws-cli:latest
    entrypoint: 
      - '/usr/bin/env'
  id_tokens:
      ID_TOKEN:
        aud: react_s3_gardenglow
  before_script:
    - cd GardenGlowDesignFrontend
  script:
    - *assume_role
    - aws s3 sync build/ s3://$S3_BUCKET
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - GardenGlowDesignFrontend/**/*
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes:
        - GardenGlowDesignFrontend/**/*
  dependencies:
    - build artifact

PlatformIO-esp32-build:
  stage: embedded_build
  image: python:3.11
  before_script:
    - pip install -U platformio
  script: "platformio run -e nodemcu-32s -d ./PixelController/"
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      when: always
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes:
        - PixelController/**/*
      when: always